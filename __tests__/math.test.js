describe("Math tests", () => {
  test("addition works", () => {
    expect(1 + 1).toBe(2);
  });

  test("multiplication works", () => {
    expect(2 * 3).toBe(6);
  });

  test("division works", () => {
    expect(10 / 2).toBe(5);
  });
});
